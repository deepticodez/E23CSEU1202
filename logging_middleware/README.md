# Logging Middleware

This folder represents the reusable logging middleware package required in the evaluation setup.

For the Frontend Track implementation, the middleware logic has been integrated directly inside:

notification_app_fe/src/middleware/logger.ts

This logger handles:
- reusable logging abstraction
- API-based logging
- graceful failure handling
- middleware-style integration across the frontend application
